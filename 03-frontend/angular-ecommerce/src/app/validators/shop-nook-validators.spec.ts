import { ShopNookValidators } from './shop-nook-validators';

describe('ShopNookValidators', () => {
  it('should create an instance', () => {
    expect(new ShopNookValidators()).toBeTruthy();
  });
});
